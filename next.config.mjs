/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.jsdelivr.net",
      "assets.aceternity.com",
      "res.cloudinary.com",
      "aceternity.com",
    ],
  },
};

export default nextConfig;
